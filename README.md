# Non-allocating Enumerables

## `EnumerableIList<T>` 

Struct wrapper for the `IList<T>` interface that provides a non-allocating enumerable for `foreach`; so that the usage can be both abstract and non-allocating for a common use.
